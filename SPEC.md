# SPP 언어 정의서 (SPP 0.1)

> SPP = **S**pecification-oriented **P**seudo-language for AI
>
> 이 문서는 SPP라는 명세 기술 언어의 문법과, AI가 SPP 파일을 실제 프로그램으로
> 번역(트랜스파일)할 때 따라야 할 규칙을 정의합니다.

---

## 0. 철학

SPP는 프로그래밍 언어가 아닙니다. **"무엇을 만들 것인가"를 모호함 없이 적는 언어**입니다.

- `.md` 파일은 **절대 직접 실행되지 않습니다.** 실행 대상이 아니라 번역 대상입니다.
- SPP 파일에는 "어떻게 구현하는가(How)"보다 "무엇을 원하는가(What)"를 우선적으로 적습니다.
  구현 방식은 AI가 대상 언어/스택의 관용적인 방식을 따라 자유롭게 결정합니다.
- 문법은 일부러 엄격하지 않습니다. 사람이 자연스럽게 쓸 수 있어야 하고, AI가 문맥으로
  빈틈을 메울 수 있어야 합니다. 대신 **섹션 구조와 핵심 키워드는 고정**되어 있어, AI가
  파일 어디에서 무엇을 찾아야 하는지 항상 알 수 있습니다.
- 모호한 부분이 남는 것은 실패가 아닙니다. SPP는 모호함을 `Open Points` 섹션에 명시적으로
  드러내고, AI가 합리적으로 채우도록 설계되어 있습니다.
- 메서드/함수의 동작은 **특정 언어의 관용구에 기대지 않고** 기술합니다. C++, C, Python,
  Java, Rust, JavaScript 등 현존하는 어떤 개발 언어(혹은 그 변형)로 구현하더라도 동일한
  결과가 재현될 수 있을 만큼 정밀해야 합니다. (3.4절 참조)

---

## 1. 패키지 (Package)

모든 SPP 명세는 **패키지** 단위로 작성됩니다. 패키지는 프로그램(또는 모듈) 하나를
설명하는 명세 전체를 가리키며, 다음 두 형태 중 하나를 취합니다.

1. **단일 파일 패키지** — `이름.md` 파일 하나가 패키지 전체를 이룹니다. 소규모
   프로그램에 적합합니다. 지금까지의 `examples/todo-cli.md`가 이 형태입니다.
2. **다중 파일 패키지** — 폴더 하나가 패키지 전체를 이루며, 그 안에 여러 `.md` 파일이
   나뉘어 존재합니다. 규모가 크거나 섹션별로 파일을 분리하고 싶을 때 사용합니다.

두 형태 모두 패키지는 하나의 `name`(Meta 섹션 참조, 2.2절)을 가지며, AI는 패키지
전체를 **하나의 번역 단위**로 취급합니다 — 파일이 여러 개로 나뉘어 있어도 결과물은
하나의 일관된 프로그램이어야 합니다.

**다중 파일 패키지의 구성.** 폴더 최상위에 반드시 `package.md` 파일을 둡니다. 이
파일이 그 패키지의 진입점(entry point)이며, `# Files` 섹션으로 나머지 어떤 파일들이
이 패키지를 구성하는지 나열합니다.

```markdown
#!specpp 0.1

# Meta

- name: my-package
- version: 0.1.0
- description: ...

# Files

- domain.md
- behavior.md
- interface.md
```

- `# Files`의 각 항목은 `package.md`가 있는 폴더 기준 상대 경로입니다.
- Meta는 `package.md`에만 둡니다 — 나열된 나머지 파일들은 패키지 전체의 이름·
  버전을 따로 갖지 않고, 자신이 담당하는 섹션(`# Domain`, `# Behavior` 등)만
  적습니다.
- 각 파일에도 2.1절의 `#!specpp` 버전 선언 줄은 그대로 필요합니다.
- 한 섹션(예: `# Domain`)이 여러 파일에 걸쳐 나뉘어도 됩니다 — AI는 `# Files`에
  나열된 모든 파일을 읽어 같은 이름의 섹션끼리는 이어붙여진 것으로 간주합니다.

> **참고:** 여러 파일이 같은 섹션을 나눠 가질 때의 세부 병합 규칙(예: 순서가
> 의미를 갖는지)은 아직 정의 중입니다.

### 1.1 외부 패키지 참조 (Import)

패키지는 다른 외부 패키지를 인용(참조)할 수 있습니다. 참조 대상은 다음 방식 중
하나로 지정합니다.

- **URI** — 예: `https://example.com/specs/auth.md` 처럼 원격 위치를 가리키는 URI.
- **경로 문자열** — **상대 경로**(예: `../shared/auth.md`) 또는 **절대 경로**
  (예: `/specs/shared/auth.md`)로 로컬(또는 같은 저장소 내) 패키지를 가리킵니다.
- **제약 조건(capability constraint)** — 특정 패키지 하나를 콕 집어 가리키는 대신,
  "이런 기능이 반드시 존재해야 하고, 이런 입력에 이런 형태의 결과를 출력해야 한다"는
  조건만 기술합니다. 이 경우 AI는 그 조건을 만족하는 라이브러리·패키지를 상황에 맞게
  **자동으로 선정**하며, 무엇을 왜 선택했는지를 생성물에 명시합니다 (4.2절 타겟 결정,
  4.5절 모호함 처리와 동일한 원칙을 따릅니다).

URI나 경로로 지정하면 **어떤 패키지**를 쓸지가 고정되고, 제약 조건으로 지정하면
**무엇을 만족해야 하는지**만 고정되고 실제 대상은 AI 재량에 맡겨집니다.

참조된 외부 패키지는 그 자체로 하나의 완결된 패키지(단일 파일 또는 다중 파일)이며,
참조하는 쪽은 그 패키지가 정의한 것(예: Domain의 클래스 등)을 가져와 재사용할 수
있습니다.

> **참고:** 참조를 어느 위치(Meta 섹션 필드 vs 전용 섹션)에 선언하는지, 참조한
> 패키지의 어떤 요소를 어떤 표기로 가져와 쓰는지에 대한 세부 문법은 아직 정의
> 중입니다. 이후 내용으로 계속 보강됩니다.

**표준 라이브러리 관례:** 이 저장소에서 표준 라이브러리 패키지는 [`std/`](std/) 폴더
아래에 `.md` 파일로 둡니다. 관련된 모듈끼리는 하위 폴더로 묶을 수 있습니다 (예:
`std/collections/set.md`) — 이때 하위 폴더는 1절의 "다중 파일 패키지"가 아니라,
그저 여러 개의 독립된 단일 파일 패키지를 모아두는 이름공간용 디렉터리입니다. 다른
패키지에서는 경로 문자열(위 참조)로 이를 가져다 씁니다 (예: 저장소 루트 기준
`/std/stdout.md`, `/std/collections/set.md`, 같은 저장소 안이라면 상대 경로도
가능).

### 1.2 패키지의 종류

패키지는 성격에 따라 두 가지로 나뉩니다.

1. **생성 대상 패키지** — 지금까지 다룬 일반적인 패키지. 아직 존재하지 않는 프로그램을
   "무엇을 만들 것인가" 관점에서 기술하며, AI가 이를 읽고 실행 가능한 코드로 새로
   번역(생성)합니다.
2. **실체 명세 패키지** — 이미 SPP가 아닌 다른 언어(C++, C, Python 등)로 구현되어 있는
   대상(라이브러리, 모듈, 바이너리 등)의 인터페이스나 동작을 기술하는 패키지. 이 패키지는
   번역(코드 생성) 대상이 아니라 **이미 존재하는 실체에 대한 설명**입니다. AI는 이 패키지의
   내용을 새 코드로 만들어내지 않고, 그 실체와 상호작용(연동·바인딩·호출)해야 하는 다른
   패키지를 번역할 때 "이 실체는 이렇게 동작한다"는 계약(contract)으로 참조합니다.
   Domain 섹션의 네임스페이스·클래스·메서드·멤버 표기(3.2~3.4절)는 바로 이런 실체를
   실제 코드와 최대한 가깝게 정밀히 옮겨 적기 위한 것이기도 합니다.

두 종류 모두 SPEC.md에서 정의한 동일한 파일 구조(2장)와 표기법(3장)을 공유합니다.
차이는 AI가 그 내용을 **새로 만들어야 할 대상**으로 보는지, **이미 존재하는 대상에 대한
설명**으로 보는지에 있습니다.

**명시 방법.** Meta의 `kind` 필드(2.2절)로 표시합니다.

```markdown
# Meta

- name: std/native/cpp/containers
- kind: native
```

- `kind: native`면 실체 명세 패키지, 생략하면(기본값) 생성 대상 패키지입니다.
- 실제 구현물의 위치(어떤 라이브러리·헤더·모듈)는 Constraints 섹션에 자연어로
  적습니다 (예: "C++ 표준 헤더 `<vector>`" — [`std/native/cpp/containers.md`](std/native/cpp/containers.md) 참고).
- 실체 명세 패키지는 보통 프로그램의 유스케이스가 아니라 API 표면 자체를
  기술하므로, `# Behavior` 섹션 없이 `# Domain`만으로 충분한 경우가 많습니다
  (2.3절 표 참조).

---

## 2. 파일 구조

패키지를 이루는 각 파일은 (단일 파일 패키지 자신이든, 다중 파일 패키지 안의 파일
하나이든) UTF-8 텍스트, 순수한 마크다운입니다. **별도의 frontmatter(YAML
메타데이터 블록) 같은 것은 없습니다** — 메타데이터도 다른 모든 내용과 마찬가지로
본문의 한 섹션(`# Meta`)일 뿐입니다. SPP는 "본문 밖의 특수한 영역"을 두지 않고,
모든 것을 동일한 섹션 구조 안에서 다룹니다. 유일한 예외는 아래 2.1절의 버전 선언
한 줄뿐입니다.

**확장자는 `.md`를 씁니다.** SPP 파일은 결국 마크다운 문서이므로, 독자적인 확장자
(예: `.spp`) 대신 흔히 쓰는 마크다운 확장자 `.md`를 그대로 씁니다 — 아무 에디터나
뷰어에서도 평범한 문서처럼 보이고 편집할 수 있습니다. 어떤 `.md` 파일이 SPP
패키지인지는 확장자가 아니라 **2.1절의 `#!specpp` 첫 줄이 있는지**로 구분합니다
— 그 줄이 없으면 그냥 평범한 마크다운 문서(예: 이 저장소의 `README.md`)입니다.

### 2.1 버전 선언 (필수, 파일 최상단)

모든 `.md` 파일의 **첫 줄**은 이 파일이 어떤 버전(또는 어느 위치)의 SPEC.md를
따르는지 선언하는 셔뱅(shebang) 스타일의 한 줄입니다.

```
#!specpp 0.1
```

또는 URL로 특정 SPEC.md 문서를 직접 가리킬 수도 있습니다.

```
#!specpp https://example.com/specpp/SPEC.md
```

- `#!specpp <version>` 또는 `#!specpp <URI>` 형식이며, 파일의 다른 모든 내용보다
  앞에 옵니다 (Meta 섹션보다도 앞).
- **모든 `.md` 파일에 필수**입니다 — Meta 섹션과 달리 생략할 수 없습니다. AI가
  본문을 해석하기 전에 어떤 버전의 규칙을 적용해야 하는지부터 알아야 하기 때문입니다.
- 버전만 쓰면(`0.1`) 이 저장소/생태계에서 통용되는 해당 버전의 SPEC.md를 뜻하고,
  URI를 쓰면 그 문서를 정확한 근거로 삼습니다.
- 이전 초안에서 Meta 섹션의 `spp` 필드로 표현하던 것을 이 한 줄로 대체합니다 —
  Meta에는 더 이상 `spp` 필드를 두지 않습니다.

### 2.2 Meta 섹션 (선택)

```markdown
# Meta

- name: todo-cli — 패키지 식별자 (kebab-case 권장)
- version: 0.1.0 — 이 패키지 자체의 버전
- description: 터미널에서 사용하는 간단한 할 일 관리 CLI
- target: 미지정 — 타겟 언어/런타임 힌트. 비워두면 AI가 상황에 맞게 제안.
- kind: (선택) — `native`면 실체 명세 패키지, 생략하면(기본값) 생성 대상
  패키지. 1.2절 참조.
- platform: (선택) — 이 패키지가 동작을 보장하는 운영체제. 비워두면 특정 OS에
  얽매이지 않는다는 뜻. 여러 개면 쉼표로 나열 (예: `windows`, 또는 `windows,
  linux, macos`). `-> fallback: ...`으로 다른 OS를 위한 대체 구현을 가리킬 수
  있다. 3.10절 참조.
- author: (선택)
- tags: (선택)
```

- `- 키: 값 — 설명` 형식은 3.4절 Domain 멤버 표기와 같은 표기법입니다. Meta도 다른
  섹션과 다르지 않은, 그저 "이 패키지 자체에 대한 값들을 나열하는" 섹션입니다.
- **Meta 섹션 자체가 선택 사항입니다.** 생략해도 되는 패키지입니다 — 특히 간단한
  단일 파일 패키지에서는 없어도 됩니다. (2.1절의 버전 선언 줄만은 예외로, 항상
  필요합니다.)
- 생략했을 때 AI는 다음과 같이 간주합니다: `name`은 파일명(또는 다중 파일 패키지의
  폴더명)에서 유추, `version`과 `target`은 미상 — 타겟은 4.2절에 따라 상황에 맞게
  결정합니다.
- Meta를 쓰기로 했다면 `name`, `version`, `description`을 채우는 것을 권장합니다.
  단, 1.1절처럼 **외부에서 참조되는 패키지**는 참조하는 쪽이 이름을 알아야 하므로
  `name`을 명시하는 것을 강하게 권장합니다.

### 2.3 본문 섹션 (고정 키워드)

본문은 마크다운 H1(`# `) 헤더로 구분된 섹션들로 이루어집니다. **섹션 이름(키워드)은 항상
영어로 고정**하여 AI가 어떤 언어로 작성된 명세든 구조를 동일하게 인식할 수 있게 합니다.
반면 **섹션 내부의 서술 내용은 어떤 자연어(한국어, 영어 등)로 써도 됩니다.**

| 섹션 | 필수 여부 | 목적 |
|---|---|---|
| `# Meta` | 선택 | 패키지 식별자, 버전, 타겟 힌트 등 패키지 자체에 대한 정보 |
| `# Intent` | 필수 | 이 프로그램이 왜 존재하는가, 어떤 문제를 푸는가 |
| `# Domain` | 프로그램에 데이터 모델이 있다면 필수 | 네임스페이스·클래스·전역 변수 등 다루는 개념과 데이터 구조 |
| `# Behavior` | 필수 (`kind: native`면 선택, 1.2절) | 프로그램이 하는 일(기능/유스케이스) |
| `# Interface` | 필수 | 프로그램이 외부에 노출되는 방식 (CLI/API/함수 등) |
| `# Constraints` | 선택 | 비기능 요구사항, 에러 처리 정책, 스타일 기준 |
| `# Examples` | 필수 | 입력→기대 결과 예제. 트랜스파일 결과 검증 기준 |
| `# Open Points` | 선택 | 의도적으로 AI 재량에 맡기는 결정 사항 |
| `# Glossary` | 선택 | 용어 정의 (도메인 용어가 많을 때) |

섹션 순서는 위 표 순서를 권장하지만 강제는 아닙니다. AI는 순서와 무관하게 헤더 이름으로
섹션을 찾습니다.

---

## 3. 표기법 (Notation)

SPP는 엄격한 BNF 문법을 강제하지 않습니다. 대신 아래의 **관용 표기(convention)** 를
일관되게 사용합니다. AI는 이 표기를 우선 해석하되, 벗어난 자연어 서술도 문맥으로
이해해야 합니다.

### 3.1 타입 표기

SPP의 타입 표기는 **C++ 스타일의 명시적 타입 선언을 기본으로 하되, 모던 스크립트
언어(TypeScript 등)의 유연한 타입 표현을 결합**합니다. 타입은 최대한 명시적으로
적는 것이 기본이지만, 유니온 타입이나 타입 추론처럼 유연한 표현도 허용합니다.

| 표기 | 의미 |
|---|---|
| `string`, `int`, `float`, `boolean` | 기본 타입 |
| `datetime`, `date` | 날짜/시각 |
| `uuid` | 고유 식별자 |
| `bytes` | 원시 이진 데이터(바이트 시퀀스) |
| `list<T>` | T의 목록 |
| `map<K, V>` | 키-값 맵 |
| `optional<T>` 또는 `T?` | 값이 없을 수 있음 |
| `enum(A \| B \| C)` | 열거형 |
| `T1 \| T2` | **유니온 타입**(TypeScript 방식) — 하나의 값이 T1 또는 T2 중 하나의 타입 |
| `T*` | T에 대한 **포인터** — 원본 데이터의 제어권(소유권)을 표현 |
| `T&` | T에 대한 **참조자** — 원본을 공유하는 별칭, 소유권은 없음 |
| `auto` | **타입 추론** — 선언 시점에 오른쪽 값으로부터 타입을 유추 (변수·상수 선언 전용) |
| `any` | 어떤 타입의 값이든 담을 수 있음(동적 타입). `auto`와 달리 파라미터·반환 타입 등 타입이 오는 어느 자리에나 쓸 수 있다 |
| `(T1, T2, ...) -> R` | **함수 타입**(콜백/델리게이트 리터럴) — 이런 시그니처의 함수를 값으로 가짐 |
| `(T1, T2, ...)` | **튜플 타입**(`->` 없음) — 이름 없는 값 여러 개를 한 번에 묶어 반환·전달할 때 씀. 이름을 붙여 재사용하고 싶으면 대신 클래스를 만든다 |
| `ClassName` | 다른 클래스에 대한 참조 |
| `ClassName<T>` | **제네릭 타입** — 타입 매개변수를 받는 클래스·인터페이스에 구체 타입을 채워 씀 |

`list<T>`, `map<K, V>`, `optional<T>`도 사실 제네릭 타입입니다. 클래스·인터페이스·
델리게이트·메서드도 이름 뒤에 `<T>`, `<K, V>`처럼 타입 매개변수를 붙여 직접 제네릭하게
선언할 수 있습니다 (3.4절 참조).

필드에 타입 표기가 없으면 AI는 멤버·변수의 설명(description)을 해석해 **가장
유사한 타입을 자동으로 결정**합니다. 명시가 항상 우선이며, 이 추론은 명시가 없을
때만 씁니다.

다만 다음 두 경우는 추측으로 얼버무리지 않고 **번역 오류**로 처리합니다 (4.5절의
일반적인 모호함 처리보다 우선하는 예외입니다 — 타입은 넘겨짚지 않습니다).

- 설명만으로는 타입을 전혀 결정할 수 없는 경우.
- 서로 양립할 수 없는 여러 타입이 동시에 필요해 보이는 경우 (예: 어떤 맥락에서는
  숫자로, 다른 맥락에서는 문자열로 쓰여야 하는데 유니온 타입으로도 자연스럽게
  설명되지 않는 경우).

이런 오류가 나면 AI는 번역을 멈추고 명세 작성자에게 타입을 명시해달라고 알립니다.

`*`(포인터)와 `&`(참조자)는 **선택적** 표기입니다 — 데이터의 원본을 누가 소유하고
누가 그저 빌려 쓰는지, 메모리 맥락을 AI에게 정밀하게 지시하고 싶을 때만 씁니다.
대상 언어에 포인터·참조 개념이 없으면(Python, JavaScript 등) AI는 그 언어의 관용적인
방식(객체 참조, 스마트 포인터, 박싱 등)으로 자유롭게 매핑합니다.

#### 변수·상수 선언

**변수(Variable)**, **전역 변수(Global Variable)**, 그리고 상수는 C++과 비슷한
명시적 선언문 형태로 씁니다.

```
타입 변수명 = 값;
```

```
int count = 0;
string title = "우유 사기";
int | string id = 1;
Task* current = null;
```

- **상수**는 재할당이 불가능한 변수로, 앞에 `const`를 붙입니다.
  ```
  const int MAX_TASKS = 1000;
  ```
- **타입 추론**이 필요하면 타입 자리에 `auto`를 씁니다.
  ```
  auto now = currentTime();
  ```

이 선언 문법은 Domain 섹션의 `## Global Variables`(3.4절)와, 메서드 안의 지역
`변수:` 선언에 씁니다. 반면 클래스의 **멤버(Member)** 는 데이터 사전(dictionary)에
가까운 `- 이름: 타입 — 설명` 표기(3.4절)를 그대로 유지합니다 — 멤버는 "선언문"이
아니라 "이 클래스가 어떤 데이터를 갖는지"에 대한 서술이기 때문입니다.

### 3.2 특성(Attribute) 표기

C#의 Attribute처럼, 선언 하나에 부가 메타데이터·제약·힌트를 붙이는 **특성(Attribute)**
표기가 있습니다. 네임스페이스, 클래스, 메서드, 멤버, 변수, 전역 변수, Feature 등
어떤 선언 앞에도 붙일 수 있습니다.

```markdown
[Deprecated]
[Serializable(format: json)]
## Class: Todo.Task
...
```

- 표기: `[이름]` 또는 `[이름(인자: 값, ...)]`. 여러 개면 줄을 바꿔 위에서부터 나열합니다.
- 표준으로 정해진 특성 목록은 없습니다. 작성자가 자유롭게 만들어 쓰고, 의미가 자명하지
  않으면 `Glossary`에 설명을 남기는 것을 권장합니다.
- AI는 특성을 장식이 아니라 **번역 시 반드시 고려해야 할 제약/힌트**로 취급합니다.
  예: `[Deprecated]`가 붙은 메서드는 새 코드에서 호출을 피하거나 경고를 남깁니다.

### 3.3 네임스페이스(Namespace) 표기

클래스와 전역 변수는 네임스페이스에 속합니다. 네임스페이스는 이름 앞에 점(`.`)으로
구분해 붙이는 **점 경로(dotted path)** 로 표기합니다.

```markdown
## Class: Todo.Storage.JsonStore
```

위는 `Todo.Storage` 네임스페이스 아래의 `JsonStore` 클래스입니다. 점 경로를 생략하면
Meta의 `name`(있다면)을 기본 네임스페이스로 간주합니다. AI는 네임스페이스를 타겟
언어의 대응 개념(네임스페이스, 모듈, 패키지, 폴더 구조 등) 중 관용적인 것으로 자유롭게
매핑합니다.

### 3.4 Domain 섹션 표기

Domain 섹션은 **클래스(Class)** 와 **전역 변수(Global Variable)** 로 구성됩니다.

```markdown
[Serializable]
## Class: Todo.Task

멤버:
- id: int — 자동 생성, 고유
- title: string — 1~200자, 필수
- done: boolean — 기본값 false

메서드:
- markDone() → void
  설명: done을 true로 바꾼다. 이미 true이면 아무 일도 하지 않는다 (멱등).
- rename(newTitle: string) → void
  설명: title을 newTitle로 교체한다.
  예외: newTitle이 공백뿐이면 ValidationError.

불변식:
- title은 공백만으로 이루어질 수 없다.
```

```markdown
## Global Variables

const int MAX_TASKS = 1000; // Todo 네임스페이스 전역에서 참조되는 최대 허용 개수
```

- `## Class: 이름` 으로 클래스를 선언합니다. 이름은 3.3절의 점 경로로 네임스페이스를
  포함할 수 있습니다. (이전 초안의 `Entity`라는 용어는 `Class`로 통일합니다 — 메서드
  없이 데이터만 있는 경우도 그냥 메서드 목록이 비어있는 `Class`일 뿐입니다.)
- **멤버(Member)** 는 클래스가 갖는 데이터입니다. `- 이름: 타입 — 설명` 형식이며,
  설명 뒤에 제약조건을 함께 적습니다.
- **생성자(Constructor)** 는 클래스 이름과 같은 이름을 갖고 반환 타입이 없는
  메서드입니다. `메서드:` 목록에 그냥 함께 적어도 되고, 구분하고 싶으면
  `생성자:` 하위 목록을 따로 둡니다.
  ```markdown
  생성자:
  - Task(title: string)
    설명: 새 Task를 만든다. id는 자동 생성, title은 인자 값, done은 false,
    createdAt은 현재 시각으로 초기화한다.
  ```
  - 파라미터 목록이 다른 생성자를 여러 개 적으면 오버로드로 취급합니다 — 호출
    시 인자 개수·타입에 맞는 것이 선택됩니다.
  - 생성자를 하나도 적지 않으면, AI는 멤버 설명으로부터 합리적인 기본 생성자를
    추론합니다 (자동 생성·기본값이 있는 멤버는 파라미터에서 빼고, 나머지는
    파라미터로 받습니다). 단, `extends`(3.4절)로 부모 클래스가 있는 경우에는
    새로 추론하지 않고 **부모의 생성자를 그대로 물려받습니다** — 예를 들어
    `## Class: ValidationError extends Exception`가 생성자를 따로 적지 않으면,
    `Exception(message: string)`을 그대로 물려받아 `ValidationError("...")`로
    만들 수 있습니다.
  - 생성자·메서드 본문(절차나 `fn` 리터럴 바디)에서 이 인스턴스 자신의 멤버를
    가리킬 때는 `this`를 씁니다 (예: `this.title = title;`).
- **정적 메서드(Static Method)** 는 특정 인스턴스가 아니라 클래스 이름으로 바로
  호출하는 메서드입니다. `[Static]` 특성(3.2절)을 붙여 나타냅니다.
  ```markdown
  메서드:
  [Static]
  - now() -> DateTime
    설명: 현재 시각을 담은 DateTime을 만들어 반환한다.
  ```
  이렇게 선언하면 `DateTime.now()`처럼 클래스 이름으로 직접 호출합니다.
  `[Static]`이 없는 메서드는 모두 인스턴스 메서드입니다.
- **메서드(Method)** 는 클래스가 갖는 동작입니다. `- 이름(파라미터: 타입, ...) →
  반환타입` 형식의 시그니처 한 줄과, 그 아래 `설명`/`절차`/`예외` 등 필요한 하위
  키(3.5절 Feature와 동일한 키 집합)를 붙여 씁니다.
- **메서드·함수의 동작은 정밀하게 기술합니다.** 반환값, 부작용(side effect), 예외
  조건, 경계값(빈 값·0·음수·null 등) 처리까지 명시해서 C++, C, Python, Java, Rust,
  JavaScript 등 현존하는 어떤 개발 언어(또는 그 변형)로 구현해도 동일한 동작이
  재현되도록 합니다. 특정 언어의 기능(예: Python의 제너레이터, C++의 참조 등)에
  기대어 설명하지 않습니다. (0장 철학 참조)
- **메서드는 `fn` 축약형(리터럴 바디)으로도 쓸 수 있습니다.** 로직이 단순해서
  `설명`/`절차`를 산문으로 나눠 적는 것이 오히려 번거로울 때, 절차 자체를 의사코드
  문장으로 바로 적는 형태입니다.
  ```
  fn 이름(파라미터: 타입, ...): 반환타입 - 한 줄 설명 {
    절차를 의사코드 문장으로 직접 기술한다;
  }
  ```
  예:
  ```
  fn run(): void - entry point {
    stdout.println("Hello World");
  }
  ```
  중괄호 안은 여전히 3.4절 마지막 항목의 "정밀 기술" 원칙을 따르는 **의사코드**이지,
  특정 언어의 실제 문법이 아닙니다. `이름(...) → 반환타입` + `설명`/`절차`/`예외`
  하위 키로 나눠 쓰는 서술형과 `fn { ... }` 리터럴 바디형은 **완전히 동등하며**,
  작성자가 더 명확한 쪽을 자유롭게 골라 씁니다. 예외 처리처럼 본문만으로 드러나지
  않는 내용은 `fn` 시그니처 아래에 `예외:` 같은 하위 키를 추가로 붙여도 됩니다.
- 클래스에 속하지 않는 **변수(Variable)** 는 메서드 하나의 실행 범위 안에서만
  존재합니다. 절차를 설명하다가 필요하면 메서드 안에 `변수:` 하위 목록으로, 3.1절의
  선언 문법을 그대로 써서 선언할 수 있습니다 (예: `변수: auto sum = 0;`). 굳이
  선언하지 않고 절차 문장 속에서 자연어로 언급해도 됩니다.
- 클래스에 속하지 않고 패키지(또는 네임스페이스) 전역에 존재하는 상태는
  **전역 변수(Global Variable)** 이며, Domain 섹션에 `## Global Variables` 목록으로
  선언합니다.
- `불변식:` (invariants) 하위 목록은 멤버 하나로 표현되지 않는 클래스 단위 규칙을
  적습니다.

**인터페이스(Interface).** (패키지 노출 방식을 적는 최상위 `# Interface` 섹션,
3.6절과는 다른 개념입니다 — 이건 Domain 안에서 클래스에 붙는 `## Interface:`
선언입니다.) C++의 추상 클래스처럼, 클래스들이 공통으로 따라야 할
계약을 `## Interface: 이름`으로 선언할 수 있습니다.

```markdown
## Interface: Todo.Storage

메서드:
- save(item: Task) -> void
- load(id: int) -> Task?
```

```markdown
## Class: Todo.JsonStore implements Todo.Storage

메서드:
- save(item: Task) -> void
  절차: ...
- load(id: int) -> Task?
  절차: ...
```

- `## Interface: 이름` 도 3.3절의 점 경로로 네임스페이스를 가집니다.
- 인터페이스의 메서드가 **시그니처만 있고 구현(절차/설명)이 없으면**, 반드시
  구현해야 하는 추상 메서드입니다 — C++의 순수 가상 함수(`= 0`)와 같은 개념입니다.
- 시그니처에 절차·설명까지 적어두면 **기본 구현이 있는 메서드**가 됩니다(C++ 추상
  클래스의 일반 가상 메서드처럼). 구현 클래스가 따로 재정의하지 않으면 그대로
  물려받습니다.
- 인터페이스도 멤버(데이터)를 가질 수 있지만, 인터페이스 자체는 인스턴스화되지
  않는 추상적인 개념입니다.
- `## Class: 이름 implements 인터페이스1, 인터페이스2`로 클래스가 하나 이상의
  인터페이스를 구현함을 표시합니다. AI는 번역 시 나열된 추상 메서드가 모두 실제로
  구현되어 있는지 확인합니다 (4.4절 검증).
- 3.9절의 `package_like`와 같은 관례("시그니처만 있으면 필수 요구사항")를 공유
  합니다. 차이는 쓰임새입니다 — `Interface`는 Domain 안에서 클래스 사이의 상속·
  다형성 관계를 표현하고, `package_like`는 주로 외부 패키지 참조(1.1절)나 전역
  계약(3.9절)에 쓰입니다.

**클래스 상속(extends).** 인터페이스가 아니라 다른 **클래스**의 멤버·메서드
(구현까지 포함해서)를 그대로 물려받고 싶을 때는 `## Class: 이름 extends 부모클래스`
를 씁니다.

```markdown
## Class: Todo.ValidationError extends Exception
```

- `extends`는 클래스 하나만 물려받을 수 있습니다(단일 상속). 여러 인터페이스를
  구현하는 `implements`와는 동시에 함께 써도 됩니다: `Class: 이름 extends 부모
  implements 인터페이스1, 인터페이스2`.
- 부모 클래스의 생성자를 호출하려면 생성자 본문(절차나 `fn` 바디)에서
  `super(...)`를 씁니다.

**콜백·델리게이트(Delegate).** 함수를 값처럼 주고받는 시그니처는 인라인으로
`(T1, T2, ...) -> R` 타입(3.1절)을 바로 쓰거나, 이름을 붙여 재사용하고 싶으면
`## Delegate:`로 선언합니다.

```markdown
## Delegate: Todo.TaskHandler(task: Task) -> void
```

이후 다른 곳에서 `Todo.TaskHandler`를 하나의 타입처럼 씁니다:

```markdown
메서드:
- onEachTask(handler: Todo.TaskHandler) -> void
  설명: 저장된 모든 Task에 대해 handler를 순서대로 호출한다.
```

**이벤트(Event).** C#의 `event` 키워드처럼, 클래스가 특정 시점에 통지를 발행하고
외부에서는 구독(subscribe)·해지(unsubscribe)만 할 수 있는 멤버는 `이벤트:` 하위
목록으로 선언합니다.

```markdown
이벤트:
- onDone: Todo.TaskHandler — Task가 완료될 때 발생한다.
```

이벤트는 멤버(Member)와 달리 외부에서 값을 직접 읽거나 대입할 수 없고, 구독자
등록·해지와 (클래스 내부에서의) 발행만 가능합니다. AI는 타겟 언어의 관용적인
이벤트·옵저버 메커니즘(C# `event`, JS `EventEmitter`/`addEventListener`, Python
콜백 리스트, Rx `Subject` 등)으로 자유롭게 매핑합니다.

**토픽(Topic)과 구독 — 리액티브 패턴.** 특정 클래스 인스턴스에 묶이지 않는 전역
발행-구독(pub-sub) 채널은 Domain에 `## Topic:`으로 선언합니다.

```markdown
## Topic: Todo.TaskCompleted(task: Task)
```

`## Topic: 이름(페이로드 타입)`으로 선언하며, 암묵적으로 `publish(payload)`와
`subscribe(handler)` 두 동작을 갖습니다. Behavior의 Feature나 Class의 메서드에서
`Todo.TaskCompleted.publish(task)`, `Todo.TaskCompleted.subscribe(handler)`처럼
참조합니다. **Event와의 차이:** 이벤트는 "이 클래스의 이 인스턴스가 통지한다"는
소유 관계가 있지만, 토픽은 발행자·구독자 모두와 무관하게 존재하는 전역 채널입니다.
AI는 타겟 언어·스택의 관용적인 pub-sub 메커니즘(메시지 버스, Rx Observable, 이벤트
버스 라이브러리 등)으로 자유롭게 매핑합니다.

**제네릭(Generic).** Class, Interface, Delegate, 그리고 메서드는 이름 뒤에
`<T>`처럼 타입 매개변수를 붙여 제네릭하게 선언할 수 있습니다.

```markdown
## Class: Todo.Box<T>

멤버:
- value: T — 보관하는 값

메서드:
- get() -> T
- set(value: T) -> void
```

쓸 때는 구체 타입을 채웁니다: `Box<Task>`, `Box<string>`. 타입 매개변수가 여러
개면 `<T, U>`처럼 쉼표로 나열합니다.

타입 매개변수에 조건(상한)을 걸고 싶으면 `<T: 상한타입>`처럼 씁니다 — 상한타입은
보통 인터페이스(3.4절)입니다.

```markdown
## Class: Todo.SortedList<T: Comparable>
```

메서드는 자신이 속한 클래스와 별개로 자기만의 타입 매개변수를 가질 수도 있습니다.

```markdown
메서드:
- map<U>(fn: (T) -> U) -> list<U>
  설명: 담긴 값에 fn을 적용해 새로운 Box<U>를 만든다.
```

### 3.5 Behavior 섹션 표기

```markdown
## Feature: 할 일 추가

설명: 사용자가 새로운 Task를 생성한다.

입력:
- title: string (필수)
- dueDate: date (선택)

절차:
1. title 유효성 검증 (Domain.Task 불변식 참조)
2. 새 Task 생성 (id 자동 생성, done=false, createdAt=현재 시각)
3. 저장소에 추가

출력:
- 성공 시: 생성된 Task 반환

예외:
- title이 비어있거나 공백뿐이면 ValidationError("title은 필수입니다")
```

- `## Feature: 이름` 으로 기능을 선언합니다.
- 하위 키(`설명`, `입력`, `절차`, `출력`, `예외`, `사전조건`, `사후조건` 등)는 필요한 것만
  자유롭게 사용합니다. 순서와 존재 여부는 강제되지 않습니다.
- `절차`는 번호 목록으로, 구현 순서의 "의도"를 나타냅니다. AI는 이를 그대로 코드
  순서로 옮기되, 대상 언어의 관용구를 따를 수 있습니다 (예: 유효성 검증을 데코레이터로
  처리하는 등). 의미가 같다면 형태는 자유입니다. 절차 단계는 Domain의 클래스 메서드를
  그대로 호출하는 형태로 적어도 됩니다 (예: "Todo.Task.markDone()을 호출한다").
- Feature도 결국 하나의 함수/진입점이므로, 3.4절의 "메서드·함수 정밀 기술" 원칙이
  똑같이 적용됩니다.
- 다른 섹션 참조 시 `Domain.Task`, `Behavior.할 일 추가` 처럼 백틱 없이 점 표기를
  써도 되고, 그냥 이름을 자연어 문장 속에서 언급해도 됩니다. AI는 문맥으로 연결합니다.

### 3.6 Interface 섹션 표기

노출 방식에 맞는 하위 헤더를 사용합니다. 예:

```markdown
## CLI

| 명령 | 설명 | 매핑되는 Feature |
|---|---|---|
| `todo add <title>` | 할 일 추가 | 할 일 추가 |
| `todo done <id>` | 완료 처리 | 할 일 완료 처리 |
| `todo list` | 목록 조회 | 할 일 목록 조회 |
```

API의 경우 `## API`에 엔드포인트 표, 라이브러리의 경우 `## Function`에 함수 시그니처
목록을 사용합니다. GUI로 노출되는 프로그램은 `## GUI`에 위젯([`std/ui/widgets.md`](std/ui/widgets.md)
등)과 그 위젯이 어떤 `Behavior`의 Feature를 실행하는지 매핑하는 표를 씁니다
(예: [`examples/rock-paper-scissors.md`](examples/rock-paper-scissors.md) 참고).
형식은 표(table)를 권장하되 목록으로 대체 가능합니다.

### 3.7 Examples 섹션 표기

```markdown
## 예제: 정상적으로 할 일 추가

입력: `todo add "우유 사기"`

기대 출력:
```
✔ 추가됨: 우유 사기 (#1)
```
```

Examples는 SPP에서 **가장 중요한 검증 도구**입니다. AI는 트랜스파일 후 이 예제들이
실제로 성립하는지 확인해야 합니다 (가능하면 자동 테스트로 변환).

### 3.8 Open Points 섹션 표기

```markdown
# Open Points

- 저장소 형식: AI가 자유롭게 결정 (권장: 로컬 JSON 파일)
- 동시성 처리: 이 버전에서는 고려하지 않음
```

명세 작성자가 "여기는 일부러 안 정했다"고 표시하는 곳입니다. 이 섹션이 없다고 해서
스펙에 빈틈이 없다는 뜻은 아니며, 그 경우 AI는 스스로 판단하고 가정을 기록합니다
(아래 4.5 참조).

### 3.9 특수 블록 구문 (Block Constructs)

지금까지의 표기는 마크다운 헤더(`## Class: ...` 등) 기반이었지만, 아래 네 가지는
`키워드 이름 { ... }` 형태의 블록 구문을 씁니다. 본문의 어느 섹션에서도 필요한 곳에
등장할 수 있으며, 보통 관련된 Class/Method/Feature 선언 바로 옆에 둡니다.

#### usage — 사용 예시

```
usage Todo.Task {
  task = Task("우유 사기")
  task.markDone()
}
```

"어떻게 쓰는가"를 보여주는 가독성용 가이드·예제입니다. `Name`은 보통 Class, Method,
Feature의 이름을 가리킵니다. 블록 안의 코드는 특정 언어 문법이 아니라 의사코드로
써도 되며, AI에게 "이렇게 쓰일 것을 염두에 두고 인터페이스를 설계하라"는 힌트로
작용합니다. `test_required`와 달리 검증 대상은 아닙니다 — 어겼다고 번역 실패로
취급하지 않습니다.

#### package_like — 추상 아키텍처 제약

```
package_like Storage {
  - save(item: Task) -> void
  - load(id: int) -> Task?
}
```

어떤 대상이 이 이름처럼 취급되려면 반드시 충족해야 하는 조건(주로 메서드 시그니처
집합)을 정의합니다. 인터페이스·프로토콜·추상 클래스에 해당하는 개념입니다. 1.1절의
"제약 조건(capability constraint)"으로 외부 패키지를 참조할 때, 그 조건을 구체적으로
정의하는 용도로도 쓰입니다 — `package_like` 블록 하나가 "이런 게 있어야 한다"는
계약 하나를 이룹니다. AI는 이 조건을 만족하는 대상을 찾거나 만들어야 합니다.

**전역 참조 축약형.** Domain 섹션(3.4절)에서 전역 변수를 선언할 때, 구체적인 타입
대신 `package_like` 계약을 타입으로 쓰고 기본 바인딩을 함께 지정하는 한 줄 축약
표기가 있습니다.

```
## globals

package_like stdout -> default: from specpp, std/stdout.md
```

- `package_like 이름 -> default: from 출처, 경로`는 "`이름`이라는 전역은 (구체
  클래스가 아니라) `package_like` 계약을 만족하는 무언가이며, 다른 것으로 채워지지
  않으면 `출처`의 `경로` 패키지를 기본값으로 쓴다"는 뜻입니다. 1.1절의 URI/경로
  참조와 제약 조건 참조를 하나로 합친 축약형입니다.
- `from specpp, ...`는 이 저장소의 표준 라이브러리(1.1절의 `std/` 관례)를 가리키는
  출처 표기입니다 — `from specpp, std/stdout.md`는 `std/stdout.md`를 기본값으로
  삼는다는 뜻입니다.
- 이 전역이 만족해야 하는 구체적인 계약(시그니처 집합)은 별도의
  `package_like Name { ... }` 블록으로 정의하거나, 기본 바인딩 패키지(`std/stdout.md`
  등)가 스스로 정의합니다.

> **참고:** `## globals`처럼 소문자 헤더나, `from` 뒤에 올 수 있는 출처 종류(저장소
> 이름, 레지스트리 등)의 정확한 문법은 계속 정의 중입니다. 지금까지는 `## Global
> Variables`(3.4절)로 써왔으므로 당분간 두 표기를 동의어로 취급합니다.

#### test_required — 무결성 검증 제약

```
test_required Todo.Task {
  - title이 200자를 초과하면 거부되어야 한다.
  - id는 음수가 될 수 없다.
}
```

반드시 통과해야 하는 경계 조건(boundary condition)을 정의합니다. 3.7절의 `Examples`가
"이렇게 동작함을 보여주는 참고 사례"라면, `test_required`는 **반드시 지켜야 하는
안전·무결성 조건**입니다 — 트랜스파일 결과가 이를 어기면 번역 실패로 간주합니다
(4.4절 검증 참조). AI는 이 조건들을 실제 테스트 코드로 옮기는 것을 권장합니다.

#### extern — 특정 언어로의 강제 번역

```
extern "C++" {
  ## Class: Todo.NativeStore
  - save(item: Task) -> void
  - load(id: int) -> Task?
}
```

이 블록 안의 내용은 **반드시 지정된 언어(`"LANG"`)로 번역**되어야 합니다 — 패키지의
전체 타겟 언어(4.2절)와 다르더라도 이 블록만은 지정된 언어를 따릅니다. `extern`은
특히 1.2절의 실체 명세 패키지와 함께 쓰여, "이 부분은 이미 존재하는 네이티브 코드와
반드시 같은 언어로 맞물려야 한다"는 것을 표시할 때 유용합니다.

**연결 방식(`how`) 지정.** 이 블록을 나머지 부분과 어떻게 연결할지는
`extern "LANG" (how) { ... }` 형태로, 언어 문자열 뒤에 괄호로 덧붙여 명시할 수
있습니다. 생략하면 AI가 상황에 맞게 자유롭게 결정합니다.

```
extern "C++" (static) { ... }
extern "C++" (static or shared or ipc) { ... }
extern "C++" (static and shared) { ... }
```

- **단일 값** — `(static)`처럼 하나만 쓰면 AI는 **그 방식만** 시도합니다. 그 방식으로
  연결할 수 없으면 대체 수단을 찾지 않고 바로 번역 오류로 처리합니다.
- **`or` 목록** — `(static or shared or ipc)`처럼 나열하면 **왼쪽부터 순서대로**
  시도하다가 처음으로 가능한 방식을 채택합니다.
- **`and` 조합** — `(static and shared)`처럼 쓰면 나열된 방식들이 **동시에(병행)**
  모두 갖춰져야 합니다. 괄호로 묶어 `or` 목록의 한 항목으로도 쓸 수 있습니다
  (예: `((static and shared) or ipc)`).
- 흔히 쓰는 값은 `static`(정적 링크), `shared`(공유/동적 라이브러리), `ipc`(프로세스
  간 통신)이지만 고정된 목록은 아닙니다 — 필요하면 다른 값을 자유롭게 써도 되고,
  뜻이 자명하지 않으면 `Glossary`에 설명을 남기는 것을 권장합니다.

> **참고:** 네 블록 모두 정확한 문법(중첩 규칙, 다른 섹션과의 결합 방식 등)은 계속
> 정의 중입니다.

### 3.10 운영체제 제약 (Platform Constraint)

Windows 레지스트리처럼 특정 운영체제에만 존재하는 기능을 기술할 때는 이 패키지
(또는 선언 하나)가 어떤 OS에서만 성립하는지 명시합니다. 두 단계가 있습니다.

**패키지 전체가 특정 OS 전용이면** Meta의 `platform` 필드(2.2절)에 적습니다.

```markdown
# Meta

- name: std/windows/registry
- platform: windows
```

**패키지 안의 일부 선언만 특정 OS 전용이면** `[Platform("os1", "os2", ...)]`
특성(3.2절)을 그 선언 바로 위에 붙입니다.

```markdown
[Platform("windows")]
- getShortcutTarget(path: string) -> string
```

- OS 이름은 소문자 문자열로 쓰며(`"windows"`, `"linux"`, `"macos"`, `"android"`,
  `"ios"` 등), 정해진 고정 목록은 아닙니다.
- 표시가 없으면 특정 OS에 얽매이지 않는다는 뜻입니다 — 지금까지 만든 대부분의
  `std/` 패키지가 여기에 해당합니다.
- AI는 트랜스파일 시점에 대상 실행 환경(OS)이 명확하면, 제약과 맞지 않는
  패키지·선언은 쓸 수 없다고 알리고 대안(다른 패키지, 조건부 분기 등)을
  찾거나 사용자에게 확인합니다 (4.2절 타겟 결정과 같은 원칙). 대상 OS 자체가
  아직 정해지지 않았다면, `platform`에 적힌 OS를 타겟 결정의 근거로 삼습니다.

**대체 구현(Fallback) 선언.** 다른 OS를 위한 대체 구현이 이미 있다면, `platform`
필드에 1.1절과 같은 `->` 화살표 표기로 그 대체 구현을 가리킵니다.

```markdown
- platform: windows -> fallback: package_like AppDirectories
  (예: std/linux/appdirs.md, std/macos/appdirs.md)
```

- `-> fallback: 값`의 값은 1.1절의 참조 방식과 같습니다 — 구체적인 패키지
  경로(여러 개면 `or`로 나열, 3.9절 extern의 목록 표기와 동일한 관례), 또는
  `package_like 이름`으로 "이 조건을 만족하는 아무 구현"을 가리킬 수도
  있습니다.
- `fallback`이 있으면 AI는 대상 OS가 이 패키지의 `platform`과 맞지 않을 때,
  오류를 내기 전에 먼저 이 대체 구현을 시도합니다. 대체 구현도 쓸 수 없으면
  4.5절의 일반적인 모호함 처리로 넘어갑니다.
- `fallback`이 없다고 해서 대체 수단이 없다는 뜻은 아닙니다 — 그저 이 패키지가
  구체적인 대안을 명시하지 않았을 뿐이며, 이 경우 AI는 평소처럼(4.5절) 판단합니다.

---

## 4. AI 트랜스파일 지침

이 섹션은 SPP의 핵심입니다. AI가 패키지를 실행 가능한 프로그램으로 번역할 때
따라야 할 절차입니다.

### 4.1 읽기 순서

패키지를 이루는 모든 파일의 `#!specpp` 버전 선언(2.1절) → Meta(있다면) → Intent →
Domain → Behavior → Interface → Constraints → Examples → Open Points 순으로 전체를
먼저 읽고 이해합니다. 부분만 보고
번역을 시작하지 않습니다. 다중 파일 패키지라면 먼저 `package.md`를 읽어 `# Files`
목록을 파악하고, 나열된 파일을 모두 읽어 패키지 전체를 파악한 뒤 번역에 들어갑니다
(1절 참조).

### 4.2 타겟 결정

`target`이 명시되어 있으면 그대로 따릅니다. 비어있거나 Meta 자체가 없으면:

1. 사용자에게 타겟 언어/스택을 물어볼 수 있으면 묻습니다.
2. 물어볼 수 없는 상황이면, `Intent`와 `Interface`를 근거로 가장 합리적인 언어/스택을
   선택하고, **생성물 상단에 선택 이유를 명시**합니다.

같은 방식으로 **대상 OS**도 정합니다 — 패키지나 참조하는 외부 패키지에
`platform`(3.10절)이 적혀 있으면 그것이 대상 OS에 대한 강한 힌트입니다. 여러
OS를 대상으로 해야 하는데 일부 기능이 특정 OS 전용이라면, OS별 분기·구현
전환은 AI가 타겟 언어의 관용적인 방식(조건부 컴파일, 런타임 분기, OS별 파일
분리 등)으로 처리합니다.

### 4.3 매핑 규칙

- `Domain`의 각 네임스페이스 → 타겟 언어의 네임스페이스/모듈/패키지 등 관용적인 대응
  개념.
- `Domain`의 각 클래스 → 타겟 언어의 자연스러운 데이터 구조(클래스, struct, dataclass
  등). 멤버는 필드/프로퍼티로, 메서드는 함수/메서드로, 불변식은 유효성 검증 로직으로
  구현합니다.
- `Domain`의 전역 변수 → 타겟 언어의 관용적인 전역/모듈 수준 상태(상수, 싱글턴,
  설정값 등)로 구현합니다.
- `Domain`의 각 인터페이스(`## Interface:`) → 타겟 언어의 인터페이스/프로토콜/추상
  클래스로 구현합니다. `implements`로 연결된 클래스가 추상 메서드를 빠짐없이
  구현하는지 확인합니다.
- Delegate·이벤트·Topic → 타겟 언어의 관용적인 콜백·이벤트·pub-sub 메커니즘(함수
  포인터, C# `event`, 옵저버, 메시지 버스 등)으로 매핑합니다.
- 특성(Attribute)이 붙어 있으면 그 의미를 번역 결과에 반영합니다 (3.2절 참조).
- `Behavior`의 각 Feature → 함수/메서드. `절차`의 단계 순서를 의미적으로 보존합니다.
  `예외`에 적힌 에러 상황은 반드시 처리합니다.
- `# Interface` 섹션(패키지 노출 방식, 3.6절) → 실제 진입점(CLI 파서, API 라우터,
  공개 함수 등). 표에 있는 각 항목이 빠짐없이 구현되어야 합니다. (Domain 안의
  `## Interface: 이름` 추상 계약과는 다른 개념입니다 — 이름은 같지만 헤더 수준과
  용도로 구분됩니다.)
- `Constraints` → 아키텍처/구현 결정에 반영합니다 (예: "외부 네트워크 접근 금지"라면
  로컬 전용 저장소만 사용).

### 4.4 검증

번역이 끝나면 `Examples` 섹션의 모든 예제를 검증합니다. 가능하면 실제로 실행해서
확인하고, 실행 환경이 없으면 코드를 따라가며(trace) 결과가 일치하는지 확인합니다.
불일치하면 스펙을 다시 확인하고 구현을 수정합니다 — 예제를 스펙에 맞지 않게 왜곡하지
않습니다.

### 4.5 모호함 처리

스펙에 명시되지 않은 결정을 내려야 할 때:

1. `Open Points`에 이미 언급되어 있으면 그 권장안을 따르되, 없으면 상식적이고 되돌리기
   쉬운(reversible) 선택을 합니다.
2. 사용자에게 물어볼 수 있는 상황이면, 트랜스파일을 멈추고 확인하는 대신, **먼저 합리적인
   기본값으로 진행하되 무엇을 가정했는지 명확히 알립니다.** 단, 그 결정이 프로그램의
   핵심 동작(Intent)을 바꿀 정도로 중요하다면 진행 전에 물어봅니다.
3. 내린 가정은 생성 결과물에 남깁니다 (코드 상단 주석, 또는 별도 `ASSUMPTIONS.md`).
   나중에 같은 스펙을 다시 번역해도 같은 결정을 재현할 수 있도록 합니다.

> **예외:** 멤버·변수의 타입을 설명만으로 결정할 수 없거나 양립 불가능한 여러
> 타입이 필요해 보이는 경우는 위 절차를 따르지 않고 곧바로 번역 오류로 처리합니다
> (3.1절 참조). 타입은 넘겨짚지 않습니다.

### 4.6 스펙과 코드가 다시 어긋났을 때

프로그램이 발전하면서 코드가 스펙보다 앞서갈 수 있습니다. 이 경우 AI는 코드를
역으로 스펙에 반영하기보다, **사람이 SPP 파일을 업데이트하도록 안내**합니다. SPP는
"현재 코드가 무엇을 하는지"의 기록이 아니라 "무엇을 원하는지"의 기록이기 때문입니다.

### 4.7 디버깅 지원 (추적성)

실행 중인 프로그램에서 오류가 났을 때, "이 동작을 명세한 스펙 어디로 가면
되는가"를 바로 찾을 수 있어야 합니다. `.md` 파일 자체는 실행되지 않으므로(0장
철학) 스펙 파일 안에 직접 중단점을 걸 수는 없지만, **생성된 코드가 항상 자신의
출처를 가리키게** 만들어서 같은 효과를 냅니다 — 디버거가 오류(예외)에서 멈추면,
그 예외가 들고 있는 출처 정보로 스펙의 정확한 위치까지 한 번에 진입할 수
있습니다.

**추적 주석.** AI는 트랜스파일 시, 스펙의 각 Class/Method/Feature로부터
생성한 선언 바로 위에 그 출처를 가리키는 주석을 남깁니다.

```
// spp-source: examples/todo-cli.md#Behavior.할 일 추가
```

- 형식은 `spp-source: 패키지 경로#섹션.이름`이며, 대상 언어의 주석 문법에 맞게
  씁니다.
- 다중 파일 패키지(1절)라면 실제로 그 섹션이 적힌 파일의 경로를 가리킵니다.

**예외의 출처 정보는 AI가 자동으로 주입합니다.**
[`std/system/exception.md`](std/system/exception.md)의 `Exception`은
`specRef` 멤버로 같은 형식의 문자열을 담을 수 있습니다. **이 값은 스펙
작성자가 절차나 `예외:` 규칙에 직접 문자열로 적는 것이 아닙니다** — `절차`나
`fn` 바디가 명세된 `예외:` 조건 때문에 오류를 던지는 코드로 트랜스파일될 때,
AI가 "지금 내가 어떤 Method/Feature를 번역하고 있는지"를 이미 알고 있으므로
그 위치를 가리키는 `specRef`를 코드 생성 시점에 자동으로 채워 넣습니다(그
Method/Feature 안에서 발생하는 모든 예외에 동일하게 적용됩니다). 스펙에는
그런 문자열이 전혀 등장하지 않아도 됩니다.

개발자는 잡은 예외의 `specRef`를 보고 바로 그 스펙 위치로 이동할 수 있습니다
— "중단점에서 진행을 누르면 실제 구현이나 스펙으로 진입한다"는 흐름은 이렇게
만들어집니다: 디버거가 예외에서 멈추면 스택 프레임(구현)과 `specRef`(스펙)
둘 중 원하는 쪽으로 들어갈 수 있습니다.

- 이 매핑을 실제 IDE·디버거의 "여기로 이동" 기능과 연결하는 것은 SPP 언어
  자체가 아니라 별도 도구(에디터 확장 등)의 몫입니다 — SPEC.md는 그 도구가
  기댈 수 있는 표준 형식(추적 주석, `specRef`)만 정의합니다.

---

## 5. 버전 관리

- 파일 첫 줄의 `#!specpp` 버전 선언(2.1절)은 이 문서(SPEC.md)의 버전(또는 정확한
  위치)을 가리킵니다. 언어 자체가 하위 호환을 깨는 방식으로 바뀌면 버전을 올립니다.
- 개별 패키지의 `version` 필드는 그 패키지 자체의 변경 이력을 추적합니다.
- 이 문서가 0.x인 동안은 실험적 단계로 간주하고, 예제로 검증하며 문법을 조정할 수
  있습니다.

---

## 6. 참고 예제

- [`examples/todo-cli.md`](examples/todo-cli.md) — 단일 파일 패키지 형태로 이 문서의
  표기법을 실제로 사용한 전체 예제입니다 (CLI 노출).
- [`examples/rock-paper-scissors.md`](examples/rock-paper-scissors.md) — `# Interface`의
  `## GUI`(3.6절)와 [`std/ui/widgets.md`](std/ui/widgets.md)(버튼 기반 위젯)를
  쓴 GUI 예제입니다.
- [`examples/2048.md`](examples/2048.md) — [`std/ui/canvas.md`](std/ui/canvas.md)
  (직접 그리는 2D 캔버스)와 `kind: native` 실체 명세
  ([`std/windows/direct2d.md`](std/windows/direct2d.md))를 함께 쓴 GUI 예제입니다.
- [`examples/notepad.md`](examples/notepad.md) — 메뉴 막대·여러 줄 텍스트 상자·
  파일 대화 상자([`std/ui/menu.md`](std/ui/menu.md),
  [`std/ui/textbox.md`](std/ui/textbox.md),
  [`std/ui/filedialog.md`](std/ui/filedialog.md))를 조합한, 더 큰 규모의
  GUI 예제입니다.
- [`examples/doom/`](examples/doom/) — id Software DOOM(1993) 엔진을 원본
  구조에 충실하게 옮긴 다중 파일 패키지 예제입니다(1절의 `package.md` +
  `# Files` 관례를 실제로 쓴 첫 예제). 렌더링은 셰이더·버텍스 버퍼 기반의
  [`std/graphics/opengl3.md`](std/graphics/opengl3.md) 또는
  [`std/windows/direct3d11.md`](std/windows/direct3d11.md)로 대상을 고를
  수 있습니다(더 단순한 레거시 즉시 모드
  [`std/graphics/opengl.md`](std/graphics/opengl.md)/
  [`std/windows/direct3d.md`](std/windows/direct3d.md)도 여전히 유효한
  대안입니다).
