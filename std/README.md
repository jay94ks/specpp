# SPP 표준 라이브러리

이 폴더는 SPP 표준 라이브러리 패키지(`.md` 파일)를 담습니다. 다른 패키지에서는
경로 문자열로 이곳의 패키지를 참조합니다 (`SPEC.md` 1.1절 참조).

패키지 전체 목록은 [`package.md`](package.md)를 보세요 — 그 파일이 SPP 자체
표기법으로 작성된 정식 색인이고, 이 README는 GitHub 등에서 폴더를 열었을 때
바로 보이는 안내문일 뿐입니다.
