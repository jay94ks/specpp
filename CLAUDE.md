# CLAUDE.md

이 저장소(**SPP**, Specification-oriented Pseudo-language for AI)에서 작업할 때
참고하는 프로젝트 지침입니다. 사람이 읽는 소개는 [`README.md`](README.md),
언어 자체의 정의는 [`SPEC.md`](SPEC.md)를 보세요 — 이 파일은 그 둘을 대체하지
않고, **이 저장소에서 반복적으로 지켜야 하는 작업 관례**만 모아둡니다.

## 먼저 읽을 것

- **[`SPEC.md`](SPEC.md)** — SPP 언어 정의서. `.md` 스펙 파일을 새로 쓰거나
  고치기 전에 전체를 읽습니다(특히 1장 패키지, 3장 표기법, 4장 AI 트랜스파일
  지침).
- **[`std/package.md`](std/package.md)** — 표준 라이브러리 전체 색인. 새
  `std/` 패키지를 추가하면 이 파일에도 함께 반영합니다.

## `std/` 패키지를 추가·수정할 때

- 각 `std/` 항목은 **독립된 단일 파일 패키지**가 기본입니다(SPEC.md 1.1절) —
  여러 파일로 나눌 필요가 있으면 다중 파일 패키지(1절)가 아니라, 같은 이름공간
  아래 여러 개의 독립된 단일 파일 패키지를 모아두는 폴더로 구성합니다(예:
  `std/native/cpp/nhttp/*.md`).
- 두 가지 패키지 종류를 구분합니다(SPEC.md 1.2절): **추상 계약**(Meta에
  `kind` 없음, 예: `std/ui/widgets.md`, `std/net/httpclient.md`)과 **실체
  명세**(`kind: native`, 이미 존재하는 실제 API를 실제 이름 그대로 옮긴 것,
  예: `std/windows/win32.md`, `std/js/axios.md`, `std/native/cpp/nhttp/*.md`).
- 제공자·플랫폼이 여러 개인 개념은 **추상 계약 1개 + 제공자별 `kind: native`
  구현 여러 개**로 만듭니다 — 구현은 제공자 이름을 딴 하위 폴더에 둡니다
  (예: `std/ui/widgets.md` + `windows/win32.md`/`web/dom.md`,
  `std/cloud/objectstorage.md` + `cloud/aws/s3.md`/`cloud/gcp/storage.md`/
  `cloud/azure/blobstorage.md`).
- **구현 사이의 실제 차이는 숨기지 말고 명시합니다.** 이 저장소의 반복되는
  설계 원칙입니다 — 예: GCP Pub/Sub이 큐·팬아웃을 한 서비스로 묶는 것, Azure
  RBAC의 권한 모델이 AWS/GCP와 근본적으로 다른 것, S3(강한 일관성)와
  DynamoDB(기본 최종 일관성)의 차이. 억지로 똑같다고 서술하지 않습니다.
- `platform:` 필드(SPEC.md 3.10절)는 고정 목록이 아닙니다 — `windows`,
  `linux`, `web` 등은 이미 유효한 값이며, 새 값이 필요하면 그냥 씁니다.

## `SPEC.md`를 수정할 때

- **`SPEC.md`는 `std/`와 그 내용물 외의 것을 언급하지 않습니다.** `examples/`,
  `README.md`, `tests/` 등 저장소의 다른 경로를 인용·링크하지 않습니다 —
  예시가 필요하면 실제 파일을 가리키는 대신 일반화된 이름(`my-package.md`,
  `Netcode.Simulation` 등)으로 씁니다. `SPEC.md`가 예제 패키지의 존재나 변경에
  결합되지 않게 하기 위한 의도적인 제약입니다.
- 같은 내용을 다른 절에서 다시 길게 풀어 쓰지 않습니다 — 이미 있는 절을
  참조합니다(예: "구체적인 기준은 3.4절 참조").

## `.md` 파일을 일괄 수정한 뒤에는 항상 검증합니다

1. **링크 검사** — 모든 `](...)` 마크다운 링크가 실제 파일로 resolve되는지
   확인합니다. 코드펜스/인라인 코드 안의 C++·JS 람다(`[](request&){...}` 등)는
   마크다운 링크처럼 보여 오탐이 날 수 있습니다 — 실제로는 코드 스팬이 보호하므로
   무시해도 됩니다(정말 코드 블록 밖인지만 확인).
2. **중괄호 균형 검사** — `test_required { ... }` 등 블록 구문이 파일마다 열고
   닫는 개수가 같은지 확인합니다.
3. **인코딩 검사** — UTF-8 치환 문자(깨진 글자)가 섞여 들어가지 않았는지
   확인합니다.

## Git

- **사용자가 그 턴에 명시적으로 요청하기 전에는 커밋·푸시하지 않습니다.**
  스펙 파일을 아무리 많이 추가·수정해도 마찬가지입니다 — 먼저 변경 요약을
  보여주고 확인을 기다립니다.
- 커밋 메시지는 **왜**(동기)를 담습니다 — 특히 이 저장소에서는 실제 제공자
  간 어떤 차이를 어떻게 반영했는지가 리뷰에 중요한 정보입니다.
